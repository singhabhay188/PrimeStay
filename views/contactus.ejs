<!DOCTYPE html>
<html>
<head>
    <title>Contact Us | Primestay</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <style>
        .animate{
            animation: moveIn 0.2s ease-in-out;
            animation-fill-mode: forwards;
        }

        @keyframes moveIn{
            0%{
                transform: translateX(110px);
            }
            100%{
                transform: translateX(-20px);
            }
        }

        #message {
            resize: none;
        }
        #contact-form input, #contact-form textarea {
            border: 1px solid #999;
        }
    </style>
</head>
<body class="w-[100vw] h-[100vh] m-0 bg-[#f5f5f5] flex flex-col justify-between">
    <%- include('./layouts/nav.ejs') %>

    <section id="contact" class="flex flex-col gap-5 px-5 md:px-12 max-w-screen-lg mx-auto">
        <h1 class="text-2xl font-semibold">Contact Us</h1>
        <p>Have questions or feedback? We'd love to hear from you! Feel free to reach out to us using the contact information below:</p>
        <div class="contact-info">
            <p>Email: info@primestay.com</p>
            <p>Phone: +1234567890</p>
            <p>Address: XYZ Colony, New Delhi, India</p>
        </div>
        
        <p class="text-xl font-semibold">Alternatively, you can use the form below to send us a message:</p>
        <form id="contact-form" class="w-full flex flex-col gap-5 py-5">
            <div class="flex items-center justify-center gap-2 flex-col sm:flex-row">
                <input type="email" placeholder="Your Email" name="email" class="p-2 w-full sm:w-1/2 rounded-lg max-w-[500px]" required/>
                <input type="text" placeholder="Your Name" name="name" class="p-2 w-full sm:w-1/2 rounded-lg max-w-[500px]" required/>
            </div>
            <textarea id="message" class="w-full p-2 rounded-lg max-w-[500px] mx-auto" name="message" required placeholder="Your Query / Suggestion" rows="3"></textarea>
            <button type="submit" class="p-2 rounded-lg bg-[#ff385c] text-white block mx-auto w-full max-w-[200px]">Join Now</button>
        </form>
    </section>

    <div id="modal-container" class="fixed bottom-[3vh] right-0 bg-black text-white rounded-xl p-5" style="transform: translateX(110%);">
        <div class="modal-content flex gap-5 items-center text-xl">
            <div class="modal-icon">&#10004;</div>
            <div class="modal-text">Successfully sent!</div>
        </div>
    </div>

    <%- include('./layouts/footer.ejs') %>

    <script>
        let contactForm = document.getElementById('contact-form');

        contactForm.addEventListener('submit', function(event) {
            event.preventDefault();

            let email = contactForm.email.value;
            let name = contactForm.name.value;
            let message = contactForm.message.value;

            console.log(email, name, message);

            let modalContainer = document.getElementById('modal-container');
            modalContainer.classList.add('animate');

            setTimeout(function() {
                contactForm.reset();
                modalContainer.classList.remove('animate');
            }, 2000);
        });
    </script>
</body>
</html>